services:
  - type: web
    name: telegram-bot
    env: java
    buildCommand: ./gradlew build
    startCommand: java -jar build/libs/telegram-bot.jar
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
    plan: free
